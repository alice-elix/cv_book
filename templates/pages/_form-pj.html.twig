{{ form_start(form) }}

	<div class="form-group">
		<div class="row">
			<div class="col-12 col-form">
			{{ form_label(form.structure_name) }}
			{{ form_widget(form.structure_name, { 'attr': {'class': 'form-control' }}  ) }}
			</div>
		</div>
	</div>

	<div class="form-group">
		<div class="row">
			<div class="col-12 col-form">
			{{ form_label(form.header_structure) }} 
			{{ form_widget(form.header_structure, { 'attr': {'class': 'form-control' }} )}}</div>
		</div>
	</div>
	<div class="form-group">
		<div class="row">
			<div class="col-12 col-form">
				{{ form_widget(form) }}
				<button type="submit" class="btn btn-login">{{ button|default('Se connecter') }}</button>
			</div>
		</div>	
	</div>
{{ form_end(form) }}
